# Talks to the simulated board over the serial port Wokwi exposes.
# Needed to run the "Run on Wokwi" task.
mpremote>=1.24

# Type stubs are NOT installed from here. They are vendored in typings/ so the
# editor understands MicroPython with no setup, locally and in a Codespace
# alike. To refresh them:
#   pip install --target typings micropython-rp2-rpi_pico-stubs==1.29.0.post1
